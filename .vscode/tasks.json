{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and test",
            "type": "shell",
            "command": "cmake . ; cmake --build . --config Release ; ctest --output-on-failure ; cpack",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}