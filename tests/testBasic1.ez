console.writeLine("a\nb");
console.writeLine(1+2);
console.writeLine(1.0+2.0);
console.writeLine(0.3+0.7);
console.writeLine("a"+"b");
console.writeLine(7/2);
console.writeLine(7%2);
console.writeLine("1"+1);
console.writeLine(1+"1");
console.writeLine(1+2*3);

let f = function { console.writeLine(args[0]); };
f("function");

if (true)  
    console.writeLine(true);
if (false)  
    console.writeLine(false);

if (1=="1")  
    console.writeLine(true);
if (1==2)  
    console.writeLine(false);

console.writeLine("s"[0]);

let i = 10;
while (i)
{
    console.write(i);
    i=i-1;
}

console.newLine();

let u;
u.p = "property";
console.writeLine(u.p);

let f2 = function { return "return " + args[0]; };
console.writeLine(f2("function 2"));

u.method = f2;
console.writeLine(u.method("method 1"));

u.method2 = function { return "this property: " + this.p; };
console.writeLine(u.method2());

let f3 = function { return f2; };

f3.prototype.callOperator = function { return this(args[0]); };
"TODO ... operator, and COMMENTS";
console.writeLine(f3().callOperator("function 3"));

import("testBasic1secondFile");
"cashe id working";
let testBasic1secondFile = import("testBasic1secondFile");
console.writeLine(testBasic1secondFile);